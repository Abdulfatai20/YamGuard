import 'package:flutter/material.dart';
class StorageTipsPage extends StatelessWidget {
  const StorageTipsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Storage tips Page',
    ));
  }
}