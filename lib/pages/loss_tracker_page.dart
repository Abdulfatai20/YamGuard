import 'package:flutter/material.dart';
class LossTrackerPage extends StatelessWidget {
  const LossTrackerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Loss Tracker Page',
    ));
  }
}